pull_request_rules:
  - name: VM6 Read legacy simulator fresh-name approval marker
    conditions:
      - base = main
    actions:
      review:
        type: APPROVE
        message: VM6-READ-LEGACY-SIM-FRESHNAME-APPROVAL
